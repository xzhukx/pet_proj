def rand_lists(chet,nechet):
    chet=[]
    nechet=[]
    a=input("range stats from")
    b=input("add last number")
    for i in range(randint(a,b)):
        if i % 2 == 0:
            chet.append(i)
        else:
            nechet.append(i)
    print(chet,nechet)
